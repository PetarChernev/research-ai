---
description: Report exploration branches, internal critiques, claims, final-audit nominations, experiments, machine checks, contradictions, and next actions.
agent: research-director
---

Produce a compact status report from durable artifacts, not conversation history. Optional emphasis:

<focus>
$ARGUMENTS
</focus>

1. Invoke `research_status` for the machine-derived summary.
2. Run `research_validate_state`; include validation failures without trying to hide or reinterpret them.
3. Read concise `research/STATE.md` for the working picture, strongest evidence, literature gaps, and next recommended actions, and `research/COMPUTATION.md` for the computational strategy the machine-derived counts cannot interpret.
4. Report the question, active exploration portfolio, active hypotheses, important claims grouped by status, internal-critique outcomes and queue, unapproved final-audit candidates from state, approved `pending` Opus claims from the ledger, active experiments, machine-check obligations with failed, inconclusive, and pending required outcomes, claims blocked by the structural computational gate, major contradictions, and the top next actions. Do not list ordinary `not-requested` claims as verification debt.
5. Flag stale or inconsistent state explicitly. Do not perform a research cycle or mutate artifacts unless the user asked for repair.
