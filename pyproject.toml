[project]
name = "physics-research-workspace"
version = "0.1.0"
description = "Lightweight helpers for an artifact-driven OpenCode physics workspace"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0.2,<7",
]

[tool.uv]
package = false
