# Research Decisions

Record consequential choices, not routine edits. Examples include abandoning a hypothesis, changing conventions, narrowing scope, selecting a numerical method, or accepting a contested source interpretation.

## Entry format

### YYYY-MM-DD: Short decision title

- **Decision:** What changed.
- **Reason:** Why this choice has the highest expected research value.
- **Evidence:** Stable claim, derivation, experiment, literature-note, or verification links.
- **Alternatives considered:** Serious alternatives and why they were not selected.
- **Reversibility:** What new evidence would trigger reconsideration.

### 2026-08-19: Make classical `4 \leftrightarrow 5` consistency the first gate

- **Decision:** Start the broader dynamical-dimensionality program with the minimal Lorentz-covariant classical `d\in\{4,5\}` state space, variational principle, and linearized consistency problem. Treat the supplied Hopf-fermion note as a hypothesis inventory and downstream benchmark, not as established theory or a manuscript ready for polishing.
- **Reason:** Every defect, spin-statistics, quantum-gravity, gauge, and phenomenology claim depends on having a well-defined state space and evolution law. Constraint closure, hyperbolicity, and resolution independence are relatively cheap and potentially decisive failure tests.
- **Evidence:** Program dependency analysis in `research/QUESTION.md`; no evidentiary claim has yet been established.
- **Alternatives considered:** (i) immediately solve the compacton/radius toy model—deferred because it cannot test covariance or interface consistency; (ii) begin with exact Hopf/FR topology—retained as a bounded audit but it cannot establish topology of an undefined full geometric configuration space; (iii) build a discrete quantum sum first—deferred because allowed histories and continuum observables are not yet defined.
- **Reversibility:** Reorder the program if literature establishes a no-go theorem for the chosen classical route, supplies an already complete alternative framework, or shows that a discrete definition can specify state space and causal amplitudes without a continuum precursor.

### 2026-08-19: Keep intrinsic and resolution descriptions as competitors

- **Decision:** Do not assume that a quotient/differential-space description and a rank-varying coframe on a smooth five-dimensional resolution are physically equivalent. Test that equivalence, including observables and gauge redundancies.
- **Reason:** A degenerate fifth direction on a fixed manifold may retain physical data that an exact quotient removes; silently equating them would beg the central dimensional-ontology question.
- **Evidence:** Distinction and required observable `resolution independence` recorded in `research/QUESTION.md`.
- **Alternatives considered:** Declare the resolution fundamental (too close to fixed-dimensional Kaluza–Klein ontology) or declare only the quotient fundamental (currently lacks a ready variational calculus).
- **Reversibility:** Merge the descriptions only after an explicit equivalence theorem or a controlled effective equivalence in the relevant regime; select one if a discriminating calculation proves the other inconsistent.

### 2026-08-19: Use the final quotient diffeology provisionally

- **Decision:** For the next foundation calculation, use the final diffeology of the explicit quotient as the provisional intrinsic smooth structure. Do not treat either ambient-horn smooth structure as resolution-independent, and keep independent `U(1)`/`SO(1,4)` connection data explicit rather than assuming collapse gauges them away.
- **Reason:** The two flat-profile horn resolutions are homeomorphic but not diffeomorphic under their canonical comparison, while the final quotient smooth algebra and form equalizer depend only on the common quotient map. Independently retained nonzero exterior curvature survives ordinary gauge, so silently dropping it would change the state space.
- **Evidence:** C007–C009 and `research/derivations/D002.md`; procedurally separated reports `research/results/verification/C007-2026-08-19.md` through `C009-2026-08-19.md` support the restricted derivations but do not independently verify them.
- **Alternatives considered:** Use an ambient-horn differential structure (rejected provisionally because it depends on the flat profile); identify all resolutions by topology alone (too coarse if circumference/coframe data are physical); omit connections at the outset (retained as a possible coframe-only theory, but not assumed for first-order gravity).
- **Reversibility:** Reconsider if the final diffeology lacks a usable integration/Stokes or causal structure, if a physically motivated action selects one embedding profile, or if a canonical reduction proves the exterior connection sector to be regular zero-charge gauge.

### 2026-08-19: Void two wrong-branch verification reports and require branch checks before delegation

- **Decision:** Treat `research/results/verification/C008-2026-08-19-anthropic.md` and `research/results/verification/C009-2026-08-19-anthropic.md` as **void, non-evidentiary artifacts**. They are excluded from the claim ledger and must not be cited. They are superseded by `C008-2026-08-19-anthropic-r2.md` and `C009-2026-08-19-anthropic-r2.md`. Every future delegation must confirm the working branch before a subagent reads or writes research artifacts.
- **Reason:** Those two reports were produced while the worktree was checked out at the `main` baseline, where the research artifacts did not exist. As a result they assert as findings that `research/derivations/D002.md` is absent and that the claim ledger is empty, and they verified claim text supplied by the task prompt rather than by a durable artifact. Both statements are false on the research branch `reseach/hopf-solitons`. Keeping them citable would inject fabricated provenance into the record.
- **Evidence:** `git status` and the branch history show the artifacts were present and committed at `03cbb8f` on the research branch; the r2 reports confirm `D002.md` is readable and the ledger populated. The void reports' own "Findings" sections state the missing-artifact claims explicitly.
- **Alternatives considered:** (i) delete them outright, initially deferred to preserve the audit trail; (ii) silently re-run and ignore them, which would hide a provenance failure; (iii) edit their frontmatter to mark them void, which would mutate an existing report, contrary to the verification protocol.
- **Resolution (2026-08-20):** After discussion, the user confirmed the reports should be deleted rather than retained: the audit trail is already preserved in this entry and in `STATE.md`, and keeping factually false files on disk (asserting the ledger and `D002.md` were missing, when they were not) risked being mistaken for evidence despite not being ledger-linked. Both files were removed via `bash rm` (the `edit` tool denies `research/results/verification/**`; `bash` requires explicit user confirmation per command, so this executed only with direct authorization, not as a permission workaround). The genuine cross-provider verifications, `C007/C008/C009-2026-08-19-anthropic-r2.md`, produced after the branch was corrected against the real `D002.md` and ledger, remain the evidentiary record for C007–C009.
- **Reversibility:** None expected; this entry is the durable record of the incident and its resolution.

### 2026-08-20: Patch legacy verification reports for the new model-provenance schema

- **Decision:** Add the required `verifier_model` and `originating_models` frontmatter fields to the four pre-schema same-model reports (`C004-`, `C007-`, `C008-`, `C009-2026-08-19.md`), without altering any other content, outcome, or finding. Both fields were set to `openai/gpt-5.6-sol` for all four, matching their existing prose disclosure of a same-model session.
- **Reason:** `research_validate_state --strict` failed with 16 structural errors because these four reports predate the `verifier_model`/`originating_models` schema requirement introduced alongside the model-bound verifier agents. The fix is a metadata migration, not a scientific change: the reports already stated in prose that they were same-model checks.
- **Evidence:** `scripts/validate_research_state.py` now returns `valid: true` with 0 errors. The 8 remaining `--strict` warnings on these same four files (`verifier model also materially produced the claim`, `verifier shares a provider with at least one originating model`) are correct and expected, matching their `"supported but not independently verified"`/`"inconclusive"` outcomes.
- **Alternatives considered:** Rewriting these reports as fresh cross-provider verifications was rejected; that would require new independent reasoning, not a metadata patch, and the genuine cross-provider checks already exist as separate `-anthropic-r2` reports for C007–C009.
- **Reversibility:** None expected; this is a permanent schema-compliance fix executed with explicit user authorization via `bash` (the `edit` tool denies this directory).

### 2026-08-19: Shift the F0 program from bare-quotient dynamics to the enriched-interface question

- **Decision:** Stop trying to derive an action from the bare final quotient calculus. Reformulate the live F0 question as: *what is the minimal enrichment beyond the bare quotient that yields a covariant, field-space-integrable interface action, and is that enrichment physical or gauge?* The next decisive calculation is the canonical/presymplectic analysis of the `D005` edge sector, not another integration-calculus attempt.
- **Reason:** `D003` (C010–C011) shows the bare final diffeology supplies no canonical measure, no stratum weights, and no nonzero collapsed-fiber trace. `D004` (C012–C013) shows the resulting five-dimensional Palatini boundary potential pushes to zero while the four-dimensional one does not, leaving an uncancelled mismatch that no pure matching multiplier can repair without forcing `P^-_{ab}=0`. `D005` (C014–C015) shows the natural covariant repair is field-space integrable only with companion coframe/reference/`eta` variations, and that the reference-free version degenerates the induced triad. The obstruction is therefore located precisely at the enrichment data, and further bare-calculus work has low information value.
- **Evidence:** `research/derivations/D003.md`, `D004.md`, `D005.md`; claims C010–C015; `research/literature/INTEGRATION_MAP.md`, which finds no established `5 -> 3` incidence operator or collapsed-fiber pushforward theorem.
- **Alternatives considered:** (i) continue searching for an intrinsic integration functional, deprioritized because both the derivation and the literature map isolate the same missing object; (ii) accept the enriched action immediately and proceed to defects, rejected because the edge sector's charge status is exactly what decides H002 versus H003; (iii) abandon the continuum route for a discrete model, premature while a nondegenerate enriched branch exists.
- **Reversibility:** Return to the intrinsic route if a stratified-integration theorem supplying collapsed incidence is found, or if the edge sector turns out to be regular zero-charge first-class, which would make the enrichment pure gauge and revive H002.

### 2026-08-20: Adopt current computational-verification semantics for legacy work

- **Decision:** Migrate the existing F0 research to the current architecture: an explicit project-specific computational strategy; claim-linked `ONNN` obligations with predeclared acceptance criteria; deterministic canonical outcomes; representation-first trust assessment; project-local computational infrastructure; research-scoped environments only when needed; Engineer provisioning only through Scientific Computation; infrastructure/environment fingerprints that make dependent results stale; and fresh model-separated verification of the complete scientific and computational evidential chain. This is a migration and revalidation pass, not the start of `D006` or another forward workstream.
- **Reason:** `D001`–`D005`, C001–C015, the source packets, and the historical verification reports predate this evidential architecture. Their reasoning remains durable evidence, but their current status must reflect what has actually been checked under the stronger procedure. The legacy ledger was also structurally shadowed by duplicate top-level YAML keys and retained the obsolete `numerical_reproduction` field.
- **Evidence:** `research/QUESTION.md`; `research/STATE.md`; `research/COMPUTATION.md`; `research/claims/ledger.yaml`; `research/derivations/D001.md`–`D005.md`; all reports under `research/results/verification/`; architecture tests and staleness semantics in `scripts/run_check.py`, `scripts/validate_research_state.py`, and `tests/test_run_check.py`.
- **Historical/status boundary:** Historical artifacts remain historical evidence and retain their original scientific content, dates, failures, outcomes, and producing/verifying model provenance. New requirements are not represented as though they existed when those artifacts were produced. Legacy `verified` status is not automatically grandfathered: C008 and C009 are moved to `derived` pending a truthful current-procedure assessment. This is **architecture/evidence-standard driven**, not scientific counterevidence and not a contradiction. Computational obligations created now are new tests of old work and are not backdated. Old independent reports remain relevant; once new computation materially changes a claim's evidential chain, a fresh verification layer must review both the old reasoning and the new computation.
- **Alternatives considered:** (i) mechanically map `numerical_reproduction` to `computational_verification`, rejected because no canonical machine obligations had run; (ii) preserve historical `verified` statuses by exception, rejected because it would overstate current entitlement; (iii) mark every legacy claim `not-applicable`, rejected because C009–C015 expose meaningful exact obligations; (iv) rewrite all old derivations/reports, rejected because it would erase history and fabricate retrospective review.
- **Reversibility:** A claim may return to or newly reach `verified` only after its currently required computation passes and an eligible independent verifier accepts the reasoning, representation, implementation, assumptions, and outcome. A status may instead fall for scientific reasons if a faithful obligation or verifier finds counterevidence. Failed and superseded checks remain durable.

### 2026-08-20: Use a minimal exact graded kernel for the migration obligations

- **Decision:** Adopt four dependency-sensitive exact obligations for C009, C010/C012, C014, and C013/C015. Keep the C009 kernel-pair check self-contained. For the three obligations that repeatedly need polynomial coefficient collection, wedge signs, field-space differentiation, epsilon parity, and exact matrix rank, provision a small standard-library exact kernel under `research/computation/exact_graded/` from the frozen contract in `research/COMPUTATION.md`. Do not install a CAS or create a separate research environment for this migration; the locked root Python runtime is sufficient. Engineer implements only this reusable substrate and its law tests after Scientific Computation provisions it; Scientific Computation validates the scientific representation and writes every claim-specific runner.
- **Reason:** The load-bearing legacy risks are exact signs, ordered antisymmetric-pair factors, `2pi` normalization, field-space companion terms, and finite rank/nullspace claims. A canonical sparse exact representation makes equality decidable with a smaller trusted surface than unrestricted symbolic simplification. Repeating these primitives separately in three claim runners would enlarge and correlate the unaudited surface, while a broad external scientific stack would add no needed operation.
- **Evidence:** Representation/trust assessment and frozen contract in `research/COMPUTATION.md`; exact mathematical objects in `D003`–`D005`; current root dependency boundary in `research/environment/README.md`.
- **Alternatives considered:** (i) use a general CAS and call `simplify`, rejected as an unnecessarily opaque equality oracle for this finite exact domain; (ii) write four unrelated one-off algebra engines, rejected because P2–P4 share conclusion-critical primitives; (iii) build a full tensor/diffeology package, rejected as speculative and outside the current obligations; (iv) require two custom implementations of every check, rejected in favor of specific alternate reconstructions that attack material shared assumptions.
- **Reversibility:** Replace or augment the kernel if a negative control, infrastructure law, scientific validation, or independent verifier exposes an encoding error. Every dependent result must then become stale and be rerun. Reassess the entire strategy before D006, moving interfaces, PDE hyperbolicity, defect numerics, or Hopf topology introduces materially different objects.

## 2026-08-21: Treat the interrupted C016 audit as no evidence and stop at the missing bulk canonical system

- **Decision:** Record C016 as `derived` with passing exact obligation O005, but leave `independent_verification: pending`. The attempted `anthropic/claude-opus-5` audit was interrupted before producing a verification report; provenance showing repository reads or check commands is not a scientific finding and will not be cited as verification. Do not infer the final gauge-versus-physical classification from the interface-only rank-ten block. Further forward computation is deferred until a bulk-plus-interface symplectic form and total Lorentz Gauss generators are defined. Any later verification retry must use the bounded audit policy: reconstruct one load-bearing inference, attempt at most three decisive attacks, inspect only the computation needed for the claim-to-representation bridge, write no code, and not rerun obligations.
- **Alternatives rejected:** Treating tool access as a partial report; promoting C016 because O005 passed; asking another verifier to approve the same incomplete packet without the missing bulk objects; or extending the finite interface matrix machinery to manufacture a gauge classification it cannot encode.
- **Why:** O005 exactly confirms the declared cut ranks, null directions, moment-map signs, shell pullback, and corner witness under D006 assumptions, but explicitly omits the bulk kinetic form, total Gauss generators, secondary constraints, and polarization. The interrupted audit supplied no durable argument. Preserving this boundary prevents a machine result from laundering an unresolved physical classification.
- **Reversibility:** A fresh eligible verifier may change the verification status only through a substantive report. A completed bulk canonical formulation may justify a new obligation and reopen the H002/H003 discriminator; the computational plan must then be redesigned for that representation.

## 2026-08-21: Preserve historical verifier outcomes during schema migration

- **Decision:** Legacy verification reports remain immutable scientific records of what their verifiers actually reviewed. Missing sections introduced by the current computational-report schema must not be filled with text that implies review of later O001–O005 obligations. Any compatibility repair must explicitly identify itself as retrospective metadata or be implemented by a validator migration rule; it must preserve the original outcome, model provenance, source packet, and absence of computational review.
- **Alternatives rejected:** Silently backdating computational review, replacing same-model reports with stronger outcomes, deleting old reports to clear validation, or treating cross-provider historical reports as fresh review of infrastructure that did not yet exist.
- **Why:** Historical reports remain useful analytic evidence, but none can establish the current claim-to-representation bridge for computational obligations created later. Altering their apparent scope would fabricate evidence and erase dependency information.
- **Reversibility:** A new model-eligible verifier may issue a new dated report against the current packet. It supplements rather than rewrites the historical report.

## 2026-08-21: Verify C016 and replace its shell shortcut with O006

- **Decision:** Accept the bounded `anthropic/claude-opus-5` audit of C016 as qualifying independent verification in the claim's fixed-interface planar regime. Retain O005 as durable evidence for its off-shell and residual-block assertions, but do not treat its hard-coded/direct-sum shell assertions as discriminating machine evidence. Adopt O006 as the required honest-shell obligation; it substitutes `lambda=-Q(e)` before differentiation and explicitly constructs the 20-dimensional shell form, cross blocks, null certificates, and moment-map contraction.
- **Reason:** The verifier independently reconstructed every load-bearing rank and sign and found no scientific counterexample, but exposed that O005 assumed two shell conclusions. O006 passed 36/36 exact assertions and detects cross-block deletion, moment-sign, factor, and independent-`delta lambda` mutations. This repairs the claim-to-computation bridge without pretending to decide the absent total Gauss reduction.
- **Evidence:** C016; `research/derivations/D006.md`; `research/results/verification/C016-2026-08-21-anthropic-r3.md`; O005 and O006 canonical results.
- **Alternatives considered:** Leave C016 below `verified` despite a successful independent reconstruction; silently reinterpret O005 as sufficient; supersede all of O005 despite its valid off-shell content; or extend O006 into a physical/gauge classification without bulk generators. All were rejected as either understating evidence, hiding a check defect, discarding useful history, or exceeding the representation.
- **Reversibility:** Demote or revise C016 if an upstream D005 audit invalidates the imported shell/potential, if O006 later fails under a faithful correction, or if a complete bulk-plus-interface canonical system changes the interpretation. The latter would require a new claim and computational phase rather than rewriting this restricted result.

## 2026-08-22: Migrate live research to the exploration and approval-gated verification lifecycle

- **Decision:** Merge the breadth-first exploration architecture into the live research branch without rewriting scientific results. Mark D001-D006 as pre-wave sequential derivations with `exploration_wave: null` and recorded OpenAI producer metadata; change ordinary independent-verification checks from `pending` to `not-requested`; retain seven pre-cutover reports in place with `legacy: true`; and reserve all future independent verification for explicitly approved Opus audits. The user confirmed that the C016 Opus audit was explicitly approved before it ran, so its report receives the missing `user_approved: true` metadata and C016 remains `verified`.
- **Reason:** The new lifecycle distinguishes routine GPT internal critique from rare final independent audit. Treating every historical `pending` check as an approved Opus queue would fabricate verification debt, while rewriting old OpenAI or Sonnet reports as current Opus artifacts would falsify their provenance. Explicit legacy handling preserves their original bodies, outcomes, source packets, and stable paths while preventing them from satisfying the current gate.
- **Evidence:** `research/derivations/D001.md`-`D006.md`; `research/claims/ledger.yaml`; all eight reports under `research/results/verification/`; the C016 approval confirmation recorded during this migration; `research/STATE.md`; and the compatibility rules in `scripts/validate_research_state.py`.
- **Alternatives considered:** Moving same-model reports into `research/critiques/` was rejected because the internal-critic role and schema did not exist when they were produced. Backfilling Opus metadata or computational-review sections was rejected as fabricated history. Demoting C016 was rejected after the user confirmed prior approval and because its report already satisfies the model-separation and substantive-reconstruction requirements. Rerunning O001-O006 was rejected because lifecycle metadata does not alter their specs, implementations, or fingerprinted infrastructure.
- **Reversibility:** Legacy reports can be superseded only by new dated evidence, never rewritten into current reports. C016 remains subject to the scientific demotion triggers in the preceding decision. A future exploration wave may build on D001-D006 but must allocate new derivations rather than retroactively assigning these artifacts to a wave.
