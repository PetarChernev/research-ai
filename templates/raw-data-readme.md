# Raw Data for {{EXPERIMENT_ID}}

Raw outputs are ignored by default to prevent accidental commits of large files. Record each input/output dataset's generating command or stable external URI, checksum, units, and schema in `../README.md`, `../config.yaml`, or `../result.json`. Force-add only small, indispensable data.
